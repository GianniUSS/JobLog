# Handover Update — 21 febbraio 2026

Questo file riassume le modifiche applicate il 21/02/2026 riguardanti la pagina di pianificazione Rentman e il backend di salvataggio.

## Backend (`app.py`)
- Migliorata la funzione `parse_iso_datetime` per supportare formati RFC2822, ISO con timezone e oggetti `datetime`.
- Normalizzazione di `break_start`/`break_end` (uso di `break_start_val`/`break_end_val`) per uniformare i tipi inviati al DB.
- Salvataggio batch reso resiliente: ogni elemento è avvolto in `try/except` separato.
- Aggiunta di log diagnostici (`app.logger.info` / `app.logger.error`).

## Frontend / Template (`templates/admin_rentman_planning.html`)
- Spostati i badge **Capo Progetto** e **Capo Squadra** nell'header principale del progetto (destra).
- Spostato il chip/toggle **Gestione Squadra (Squadra/Individuale)** nell'header della card **Fasi** e allineato a destra; aggiunta fallback per progetti senza fasi.
- Corretto tag chiuso duplicato introdotto precedentemente.
- CSS: ampliato il campo nome funzione nella modal `Configura Fasi Funzione` (`.fn-template-header input { min-width: 420px; max-width: 680px; }`).

## Verifiche
- Endpoint `/api/admin/rentman-planning/save` risponde `200` sui test eseguiti.
- Query MySQL confermano la persistenza di `custom_location_ids` (es. `[73]`, `[71]`).

## Note
- Se desideri una larghezza diversa per il campo funzione (più largo o più stretto), indicami il valore e lo applico.
- Consiglio un controllo responsive su mobile/tablet per la modal.
